{% extends 'template.html.twig' %}

{% block body %}
    
    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.firtname) }}</div>
        <div class="col">{{ form_row(form.name) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block PageTitle %}

    {% if (new==1) %}
        Ajouter une personne
    {% endif %}

    {% if (new==0) %}
        Editer une personne
    {% endif %}
    
{% endblock %}

{%  block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}